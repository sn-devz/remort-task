import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  modalContainer: {
    flex: 1,
    position: 'absolute',
    height: '100%',
    
  },
  modalSubContainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  childContainerBorder:{borderRadius: 8}
});
